fastapi==0.115.12      # High-performance ASGI framework
httpx==0.28.1          # Async HTTP-Client für die Proxy-Aufrufe
uvicorn[standard]==0.34.2  # ASGI-Server mit uvloop & httptools Extras
python-dotenv==1.1.0   # Laden von Umgebungsvariablen aus .env
